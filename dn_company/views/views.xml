<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="dn_company.list">
      <field name="name">dn_company list</field>
      <field name="model">dn_company.dn_company</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="dn_company.action_window">
      <field name="name">dn_company window</field>
      <field name="res_model">dn_company.dn_company</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="dn_company.action_server">
      <field name="name">dn_company server</field>
      <field name="model_id" ref="model_dn_company_dn_company"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="dn_company" id="dn_company.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="dn_company.menu_1" parent="dn_company.menu_root"/>
    <menuitem name="Menu 2" id="dn_company.menu_2" parent="dn_company.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="dn_company.menu_1_list" parent="dn_company.menu_1"
              action="dn_company.action_window"/>
    <menuitem name="Server to list" id="dn_company" parent="dn_company.menu_2"
              action="dn_company.action_server"/>
    -->
  </data>
</odoo>