<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="dn_employee.list">
      <field name="name">dn_employee list</field>
      <field name="model">dn_employee.dn_employee</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="dn_employee.action_window">
      <field name="name">dn_employee window</field>
      <field name="res_model">dn_employee.dn_employee</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="dn_employee.action_server">
      <field name="name">dn_employee server</field>
      <field name="model_id" ref="model_dn_employee_dn_employee"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="dn_employee" id="dn_employee.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="dn_employee.menu_1" parent="dn_employee.menu_root"/>
    <menuitem name="Menu 2" id="dn_employee.menu_2" parent="dn_employee.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="dn_employee.menu_1_list" parent="dn_employee.menu_1"
              action="dn_employee.action_window"/>
    <menuitem name="Server to list" id="dn_employee" parent="dn_employee.menu_2"
              action="dn_employee.action_server"/>
    -->
  </data>
</odoo>