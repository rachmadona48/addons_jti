<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="uc_task.list">
      <field name="name">uc_task list</field>
      <field name="model">uc_task.uc_task</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="uc_task.action_window">
      <field name="name">uc_task window</field>
      <field name="res_model">uc_task.uc_task</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="uc_task.action_server">
      <field name="name">uc_task server</field>
      <field name="model_id" ref="model_uc_task_uc_task"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="uc_task" id="uc_task.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="uc_task.menu_1" parent="uc_task.menu_root"/>
    <menuitem name="Menu 2" id="uc_task.menu_2" parent="uc_task.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="uc_task.menu_1_list" parent="uc_task.menu_1"
              action="uc_task.action_window"/>
    <menuitem name="Server to list" id="uc_task" parent="uc_task.menu_2"
              action="uc_task.action_server"/>
    -->
  </data>
</odoo>